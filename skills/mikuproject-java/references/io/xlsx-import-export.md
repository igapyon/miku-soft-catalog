# XLSX Import Export

`XLSX` は対外的な受け渡しや編集素材として扱います。

- export は `export-xlsx` または `export-wbs-xlsx` のどちらを主に使うか今後整理する
- import は `import-xlsx`
- structural workbook `XLSX` と report 系 `WBS XLSX` は混同しない
